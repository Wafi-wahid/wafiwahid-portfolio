/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  // Set basePath later for GitHub project pages, e.g. "/wafiwahid-portfolio"
  // basePath: "",
  // assetPrefix: "",
};

export default nextConfig;
